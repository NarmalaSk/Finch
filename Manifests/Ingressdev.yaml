apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: finch-api-ingress
  annotations:
    kubernetes.io/ingress.class: "gce"     # For GKE
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: finch-dev-service
                port:
                  number: 80
